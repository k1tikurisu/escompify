generator client {
  provider = "prisma-client-js"
  output   = "/works/src/__generated__/client"
}

datasource db {
  provider = "sqlite"
  url      = env("DATABASE_URL")
}

model GumTree {
  id       Int    @id @default(autoincrement())
  key      String @unique
  serverId Int?
  status   String
  srcAst   String
  dstAst   String
  actions  String
}
